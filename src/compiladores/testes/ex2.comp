program
 int a, c;
 int d, e;
 a = 0; d = 5;
 c = d / 1;

 scan (a);
 scan (c);
 b = a * a;
 c = b + a * (1 + a*c);
 print ("Resultado: ");
 print (c);
 a = (b + c + d) / x;
 e = val + c + a;
 print ("E: ");
 print (e);
end